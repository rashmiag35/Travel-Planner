# Travel-Planner

By default, the OpenWeatherMap API returns temperature values in Kelvin, so 273.15 is used to convert Kelvin to Celsius.
The OpenWeatherMap API returns the visibility value in meters, so dividing by 1000 converts meters to kilometers.
